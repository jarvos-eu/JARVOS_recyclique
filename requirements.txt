# RecyClique — dépendances Python (dev local et tests)
# Docker utilise api/requirements.txt (voir Dockerfile). Ici : même base que l’API + pytest/httpx pour les tests.
# Installer en local : pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.0.0
redis>=5.0.0

# Tests
pytest>=8.0.0
httpx>=0.27.0
