/**
 * Visual — interfaces et fournisseurs de visuels (v1 stubs, v2+ Peintre).
 */
export type { VisualContext, VisualProvider, VisualResult } from './types';
export { visualProviderStub } from './visual-provider.stub';
export { useVisual } from './useVisual';
