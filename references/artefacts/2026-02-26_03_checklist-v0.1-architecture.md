# Checklist v0.1 - points à trancher / intégrer (architecture)

Date : 2026-02-26  
Source : `_bmad-output/planning-artifacts/architecture.md`, sections Gap Analysis et Implementation Readiness.

**Pour qui** : Strophe et agents (PM, SM, Dev) lors des premières stories / socle. À charger en contexte quand on travaille sur les épics v0.1 (socle, structure, modules).

---

## À faire en v0.1 (ne pas oublier)

- [x] **Loader modules (TOML, ModuleBase) et slots** : intégrer dans la structure lors des **premières stories modulaires**. — **Fait (Story 1.4)** : `api/config/modules.toml` + `api/core/modules/` (loader, base, stub), `api/workers/`, `frontend/src/shared/slots/` (voir `shared/slots/README.md`).
- [x] **Convention tests frontend** : trancher une fois pour toute l'app. — **Tranché** : tests **co-locés** `*.test.tsx` (voir `frontend/README.md` et architecture § Important Gaps).
- [x] **Outil tests frontend** : Vitest + React Testing Library + jsdom. — **Fait (Epic 1)** : `frontend/package.json` (scripts test, test:run), `frontend/README.md` (§ Tests). Pas de Jest ni autre runner. E2E : hors périmètre v0.1 ; si E2E plus tard = Playwright (référence 1.4.4).
- [x] **Versions Python et Node** : inscrire dans Dockerfile et README. — **Fait** : Python 3.12, Node 20 LTS dans Dockerfile ; tableau des versions dans README racine + doc/deployment.md.
- [ ] **Détail module correspondance (FR13b)** : affiner champs et règles après confrontation BDD + instance dev + analyste. **Reporté** à l'Epic 3 (stories correspondance) ; ne pas bloquer l'Epic 2.
- [x] **Styling / UI frontend** : **Mantine** (alignement RecyClique 1.4.4 pour réimport du visuel). — **Tranché** : composants UI et thème Mantine ; version à figer à l'introduction. Référence : `references/ancien-repo/technology-stack.md`. Ne pas introduire Tailwind ni autre lib UI.
- [x] **Déploiement effectif** : pour les stories qui livrent Docker/déploiement, exécuter `docker compose up` + vérifier /health (ou documenter vérification manuelle). — **Règle** : Story 1.5 + artefact `references/artefacts/2026-02-26_05_regle-deploiement-effectif-stories-docker.md`.

---

## Qui fait quoi — handoff (rétro Epic 1, 2026-02-26)

Voir le document de rétrospective et la section « Liste de tout ce qu'il y a à faire » pour les tâches à exécuter par Strophe ou par un autre agent/workflow.

Référence : `_bmad-output/implementation-artifacts/epic-1-retro-2026-02-26.md`.

---

## Référence

- Architecture complète : `_bmad-output/planning-artifacts/architecture.md`, sections « Gap Analysis Results », « Implementation Readiness Validation », « Implementation Handoff ».
