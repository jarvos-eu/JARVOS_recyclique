# Exemple de variables d'environnement pour RecyClique (Docker Compose et exécution locale)
# Copier ce fichier vers .env et renseigner les valeurs (ne pas committer .env).
# Aucune valeur sensible ici : à usage documentaire uniquement.

# Base de données RecyClique (PostgreSQL)
# En Docker Compose, DATABASE_URL est construit automatiquement ; override possible ici.
# DATABASE_URL=postgresql://recyclic:MOT_DE_PASSE@postgres:5432/recyclic

# Redis (EventBus, file push)
# En Docker Compose, REDIS_URL est défini automatiquement ; override possible ici.
# REDIS_URL=redis://redis:6379/0

# Mot de passe PostgreSQL (utilisé par le service postgres et pour construire DATABASE_URL)
# Définir en production ; valeur par défaut en dev : recyclic
# POSTGRES_PASSWORD=recyclic

# Optionnel : CORS (origines autorisées, séparées par des virgules)
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optionnel : mode debug
# DEBUG=false
